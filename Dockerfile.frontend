# Frontend Dockerfile for Vite preview server
FROM node:20-alpine AS build
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm install
COPY . .
RUN npm run build

EXPOSE 5190
CMD ["npm", "run", "preview", "--", "--host", "0.0.0.0", "--port", "5190"]